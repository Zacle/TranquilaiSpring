controller:
  replicaCount: 2
  admissionWebhooks:
    enabled: true
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
  service:
    annotations:
      service.beta.kubernetes.io/do-loadbalancer-name: tranquilai-ingress
      service.beta.kubernetes.io/do-loadbalancer-protocol: http
      service.beta.kubernetes.io/do-loadbalancer-tls-ports: "443"
  config:
    use-forwarded-headers: "true"
    compute-full-forwarded-for: "true"
    proxy-body-size: 10m
