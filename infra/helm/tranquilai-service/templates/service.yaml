apiVersion: v1
kind: Service
metadata:
  name: {{ include "tranquilai-service.fullname" . }}
  labels:
    {{- include "tranquilai-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tranquilai-service.selectorLabels" . | nindent 4 }}
